import 'package:flutter/material.dart';

class AppConstants {
  static const appName = "DiD";
  static const appBackGroundColor = Color.fromRGBO(203, 203, 203, 1);
  static const appSecondaryColor = Color.fromRGBO(220, 220, 220, 1);
}
